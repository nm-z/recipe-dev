# Recipe issue evidence, September 24, 2026

This file records issues addressed during the eight-hour pass. “Verified” means the named public path was observed on the listed device; it does not imply every backend. All issues were left open at the pass handoff. Source commits are on `origin/minimal` unless identified as pending.

On September 24 at 13:47 PDT, 21 issues previously identified as resolved on tested paths were closed at Nate's request: #906, #913, #915, #916, #918, #921, #922, #925, #927, #928, #930, #931, #936, #937, #938, #939, #943, #946, #948, #949, and #950. GitHub returned `state=closed` and `state_reason=completed` for each. Read-only before and after API responses and the close receipt are in `/home/nate/codex/recipe-issue-close-review/`. The device and case limits below still apply.

| Issue | Result and exact scope |
|---|---|
| #905 | AMD signal wait checks for actual zero after each wake. AMD-cfg compile passed on Archy. AMD runtime unavailable. Commit `a91150e6`. |
| #906 | Residual add and skip projection use the residual precision. `res_precision.rs` observed BF16 residual and following FP16 normalization on Archy CPU and nv0. Commit `21431446`. |
| #910 | Requantization retains original mapped bytes for a second conversion. Old `.qi` reproduction uses removed API; current public reproduction pending. Commit `a91150e6`. |
| #911 | Implicit final projection, MoE routers, and frozen estimator channel copies use real precision under an int8 sum table. `implicit_output.rs` loss decreased on Archy CPU/nv0 with `RECIPE_CONFIG=llamacpp`; T4 packets at cursors 40017/40056 and estimator packets at 40088/40102/40103, plus Archy CPU packet 40178, share these causes. Cursors 40017/40056/40178 replayed without packets on Archy CPU/nv0; Azure T4 run `35993672192` replayed 40080–40119 with zero packets, covering all three estimator failures. Other generated weighted paths are not exhausted. Commits `f7c95225` and `cf6d19a3`. |
| #913 | Recurrent body weight and gradient offsets use padded plan offsets. `recur_place.rs` 33-position/width-3 sequence changed all 64 predictions and reduced loss on Archy CPU/nv0. Commit `21431446`. |
| #918 | Positional script arguments reach local and all-remote execution. `args.rs` checked on Archy/Benji; RNJ K80 one-shot positional prompt answered “papaya.” Commit `a91150e6`. |
| #919 | The current `minimal` checkout contains `rnj-1.rs` with the new `.chat` entrypoint but no `rnj-chat/server.mjs` or `start.sh`; `/home/nate/codex/rnj-chat` contains runtime files, not that server source. The old external server protocol remains unverified and this issue is open. |
| #920 | Graph append/split retains the requantize vector. `recur_place.rs` trained/saved/placed/inferred on Archy CPU/nv0. Explicit two-device split hit unrelated connection-cut validation, so that split remains unproven. Commit `a91150e6`. |
| #921 | Attention returns a full Block. `attention_api.rs` compiled and ran on Archy CPU/nv0. Commit `d7f082ef`. |
| #922 | Attention index/gate suffixes retain their selectors. `attention_api.rs` observed attention FP16 and pool FP64 on Archy CPU/nv0. Commit `d7f082ef`. |
| #925 | Recurrent sigmoid call names the precision variant. `recur_sigmoid.rs` changed all 64 predictions and reduced loss on Archy CPU/nv0. Commit `f7c95225`. |
| #927 | GGUF epsilon is resolved when the model binds the file, even if `model()` came first. `epsilon_order.rs` RNJ K80 produced 128,256 logits bitwise equal to explicit file epsilon and distinct from explicit Cargo epsilon. Commit `f7c95225`. |
| #928 | Frozen product/hyper qualifiers propagate to nested parameters. `frozen_product.rs` changed 0/128 frozen predictions versus 128/128 trainable controls on Archy CPU/nv0. Commit `a91150e6`. |
| #929 | The filed `.qi(8).0.fp(16)` public reproduction no longer compiles because `.qi` was removed. The unpacked device requantizer still uses an unrounded float scale internally; no current public path to that specific storage selector was established. Issue remains open. |
| #930 | Selected Jinja raise reported `boom` through `template_probe.rs` on Archy CPU with a small GGUF. Commit `a91150e6`. |
| #931 | Same-line Jinja block preserved whitespace and produced input IDs `[72,105,32]` through `template_probe.rs` on Archy CPU. Commit `a91150e6`. |
| #939 | Skipped Jinja conditional branch did not raise in `template_probe.rs` on Archy CPU. Commit `a91150e6`. |
| #940 | The filed assertion still compares a one-element size against the six-element metrics allocation. Repository instructions ban inline Rust tests and separate test-only paths; no source edit or public replacement check was completed during this pass. Issue remains open. |
| #932 | AdamW skips stored-bound weights. Ordinary int8 training progresses in `q8_tie.rs`; bound training path remains unreachable for a direct runtime check. Commit `a91150e6`. |
| #933 | Quantized extrema tie-break matches signed magnitude ordering. `q8_tie.rs` trained and predicted on Archy CPU/nv0 at steps 32 and 256; AMD 64-lane path unverified. Commit `a91150e6`. |
| #934 | TopK backward maximum uses model-score precision. Source fix only. `Primitive::TopK` is reached through the private GGUF MoE lowering path, while public `moe` branches use another selector; no MoE GGUF was found in Archy's installed model paths for a mixed-precision runtime check. Commit `a91150e6`. |
| #935 | Gate/up tensor assignment preserves left-to-right binding. RNJ K80 yielded 128,256 byte-identical logits for commuted activated product; `recipe stats` showed distinct gate/up statistics before its bound expired. Commit `a91150e6`. |
| #936 | Online attention rejects head width above 256 before dispatch. `online_guard.rs` width 256 inferred and width 512 failed explicitly on Archy CPU/nv0. Commit `a91150e6`. |
| #937 | ReLU activation precision fusion requires matching formats. `relu_precision.rs` observed separate FP32/FP16 and FP16/FP32 nodes, same-format fusion, and outputs on Archy CPU/nv0. Commit `a91150e6`. |
| #938 | R² uses centered device mean/M2 merged across batches. `r2_centered.rs` showed nonzero R² on 9999/10001 imperfect targets and zero on constant targets with 512-row batches, Archy CPU/nv0. `r2_perfect.rs` then generated nonconstant targets from a seeded public model and observed identical 8/8 prediction bits, loss 0, and R² 1 on both CPU and nv0. Commit `a91150e6`. |
| #943 | CLI uses `--ctx`, `--cfg`, `-p`, and device-only worker startup. Public flags script, worker handshake, and Archy-to-Benji chain passed. Commit `a91150e6`. |
| #946 | `.budget` and saved budget fields removed; `--ctx` is explicit capacity. RNJ K80 --ctx64 completed 31 input plus 9 output tokens; new attention bundle reloaded; old format rejected explicitly. Commit `a91150e6`. |
| #947 | Still open. Current attention gate multiplies a gate plane before the attention output projection; ordinary `Block * Block` multiplies two completed block outputs. Replacing the selector directly would change the GGUF checkpoint math and tensor binding. The README deletion of `.gate()` is Nate's preserved unstaged edit, not a published API change. |
| #948 | Public generic activation name removed; named activation syntax exercised on Archy CPU/nv0. Commit `a91150e6`. |
| #949 | Second generic activation name removed; named activation syntax exercised on Archy CPU/nv0. Commit `a91150e6`. |
| #950 | `.e(value)` installed and old `.epsilon` removed; save/reload exercised on Archy CPU/nv0. Commit `a91150e6`. |
| #900 | `ptxas` use now checks target support after release/driver compatibility. Actual `/opt/cuda/bin/ptxas -arch=sm_37 --version` failed and sm_52/sm_70/sm_75 succeeded on Archy; normal nv0 suite passed. The supported-toolkit/driver branch is unverified; driver-JIT warmup is not implemented. Commit `a32ed5f6`. |
| #901 | NVIDIA integrated-device exclusion removed. Archy discrete nv0 suite passed 8/8; no integrated NVIDIA runtime available. Commit `a32ed5f6`. |
| #854 | The original packet used removed `Train::fp` and `recipe.infer(bundle, input)` APIs. A current public analogue with `attn(3).width(8).fp(32)` trained, saved, inferred, and passed the harness at cursor 15658 on Archy nv3 without a synchronization error. The original exact path cannot be replayed under the current API; issue remains open. |
| #855 | The original packet used removed `.qi`, `Train::bf`, and old `recipe.infer(bundle, input)` APIs. A current public BF16 attention analogue trained, saved, inferred, and passed the harness at cursor 15656 on Archy nv1 without the filed invalid i16 LLVM constant. The original exact path cannot be replayed; issue remains open. |
| #879 | The original FP8 failure used removed `.qi` syntax and occurred during tuning. A current public FP8 composition on Archy CPU and nv3 rejects before dispatch with `has no fp8 instruction`, rather than producing a nonfinite objective. FP8-capable hardware and full current FP8 training remain unverified. |
| #903 | Current capability table selects `dp4a` for NVIDIA sm_61 and newer. Source inspection indicates the filed unconditional false capability is already fixed; target-specific performance remains unmeasured. |
| #892 | KFD stepping formatted as a hex digit, so 90010 maps to gfx90a in source. AMD-cfg check passed; AMD runtime unavailable. Commit `a32ed5f6`. |
| #896 | gfx12 WMMA capability row now matches gfx1200/gfx1201 exactly; gfx1250 falls through to the vector row. AMD-cfg check passed; gfx1250 runtime unavailable. Commit `a32ed5f6`. |
| #898 | Cooperative residency uses a conservative per-generation VGPR file and allocation granule, and rejects a kernel larger than the SIMD file. AMD-cfg check passed; AMD runtime unavailable. Commit `a32ed5f6`. |
| #907 | Current decoder state-helper calls include their variant suffix; source inspection indicates the filed defect is already fixed. Runtime mixed-precision decoder case pending. |
| #908 | RAT evaluator weights now write each scorer node at `NativeLayout.weights[index]` in that node's precision, with source-span bounds and a complete-count check. The public command RAT case ran on Archy CPU/nv0 for FP16 and FP32 evaluators; the FP32 loss changed from 0.887922 before to 0.876020 after. A direct numerical oracle for the fitted scorer weights remains unavailable through the public report. Commit `c8eb132d`. |
| #909 | Current gradient reduction calls use `@reduce_rows_state{v}`; source inspection indicates the filed unsuffixed calls are gone. Runtime mixed-precision case pending. |
| #912 | `backend_template` drops `default` before a named fp64 cache suffix; source inspection indicates the filed template-key defect is already fixed. Runtime fp64-cache case pending. |
| #914 | Recurrent body copies the outer profile. `recur_sigmoid.rs` exercised one fp32 body on Archy CPU/nv0. A current public FP32 cell with an FP16 body failed explicitly before dispatch with `no accumulator-width backward kernel`, rather than silently reading mixed weight bytes. Other per-node mixed formats and full mixed-precision support remain unverified. |
| #916 | The checked-in composition harness now passes a precision to `model(cursor)` and generates current integer precision calls instead of removed `Qi` fields. Its public runner executed cursor 40000 on Archy CPU and Azure T4, and later bounded Azure batches completed hundreds of compositions. Commit `a273ea4c`. |
| #926 | Attention now selects the matrix body only when its cache precision matches the node precision, matching the linked variant template. AMD-cfg compilation passed on Archy; gfx11 runtime unavailable. Commit `1edea823`. |
| #924 | RAT sample writes now use the compiled tape input precision in fit, predict, stateful episode, selector, and backward paths. Public command RAT with an FP16 evaluator failed before with `GPU byte write exceeds buffer`, then completed on Archy CPU/nv0 after the fix; FP32 and `--cfg llamacpp` CPU controls also completed. Stateful and learned policy paths remain unverified. Commit `c8eb132d`. |
| #915 | A bundle trained under `llamacpp` silently changed all 65 sampled prediction bit patterns when reopened under `recipe` before the fix. The current source stores the full resolved precision table in the saved artifact identity, compares it before predict/place/resume, and rejects a mismatched table or old bundle. Same-table training, prediction, and the 8/8 save/resume suite passed on Archy CPU/nv0; different-table predict/resume exited with the explicit profile error. Commit `cf6d19a3`. |
| #917 | README no longer contains the filed `.qi` example, and `.qi` is no longer public. Source inspection indicates the filed documentation case is obsolete. |
| #923 | `.f(e,m)` is no longer public; source inspection indicates the filed variant-collision case is obsolete. |
| #885 | The reported FP64 Ctrl-C hang occurred only on an AMD gfx1101 machine. This pass changed AMD wait/residency logic in #905/#898 but had no verified AMD runtime route for an interrupt reproduction. The issue remains open; no cleanup or process termination was attempted on Nate's desktop. |
| #881 | The original nv0 epoch-8 hang composition uses removed `.qi` selectors. Current public GRU/ensemble sweeps on Archy NVIDIA completed, but they are not an exact reproduction of this old packet. Issue remains open. |
| #878 | The filed Bayes/pool/perceptron constant-loss composition uses removed `.qi` selectors. Its exact public source no longer compiles; a current equivalent with the same stored-layout choices was not established. Issue remains open. |
| #877 | The old quantized MoE/GRU inference timeout uses removed `.qi` selectors. Current MoE compositions passed in remote sweeps, but the original layout cannot be replayed under the current API. Issue remains open. |
| #875 | The GRU/residual/KMeans nonfinite packet uses removed `.qi` selectors. Its exact source no longer compiles; current residual and recurrent sweeps are different models. Issue remains open. |
| #872 | LSTM dispatch hang was reported on AMD. No verified AMD runtime machine was available during this pass, so neither reproduction nor a completion claim is possible. Issue remains open. |
| #775 | The filed nv3 timeout used removed `.quantize`, `.act`, `Train::fp`, and old saved-model inference APIs; its own packet had no replay and `stable:false`. A current exact-source repeat is impossible without redefining its math. Issue remains open. |

## Remaining work to review

1. #947 needs an attention representation that multiplies the gate before the output projection and still binds GGUF gate/QKV/output tensors in the correct order. Removing the selector directly would alter model math. The preserved README edit is not published.
2. #915 deliberately rejects old bundles whose precision profile was not recorded. A migration would need a trustworthy source profile; silent reinterpretation is not acceptable.
3. #910, #920, #929, #932, #934, and #914 have source fixes, stale reproducers, or explicit failures, but the specific remaining user paths in the table above lack complete runtime proof.
4. #919 refers to an external chat server whose source is absent from this checkout. The canonical RNJ terminal chat path passed on Archy K80; the old server protocol remains open.
5. #900 still pays cold driver PTX JIT when the toolkit assembler is incompatible. Modern NVIDIA targets in #899, shared-memory opt-in in #902, and integrated NVIDIA hardware in #901 lack an available runtime machine for verification.
6. AMD runtime verification remains unavailable for #905, #892, #896, #898, and #926. The separate AMD queue still includes #889, #890, #891, #893, #894, #895, and #897 for older waves, matrix cores, APUs, and non-Linux support.
7. The model-topology log in #886, the wider API redesign in #887, #888, #944, and #945, the GPU residency enhancement #941, and model/decode enhancements #262, #562, #567, #571, #670, #671, #673, #674, #675, and #679 remain open. #940's inline test assertion also remains under the repository's no-inline-tests rule.
